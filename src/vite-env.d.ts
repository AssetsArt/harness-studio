/// <reference types="vite/client" />

// Injected by Vite's `define` (see vite.config.ts) — the running plugin version,
// read from .claude-plugin/plugin.json at dev-server start and shown in the Topbar.
declare const __ARTA_VERSION__: string;
